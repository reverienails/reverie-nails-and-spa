// Hero images for the main homepage hero section (the large photo next to "Thoughtful care.").
// A random photo from the folder is chosen automatically every time the page loads.
//
// The list of possible hero photos is now in the main JS (for auto support of common names).
// Drop any .jpg or .png into the assets/Hero/ folder with a name matching the patterns
// (e.g. hero-3.jpg, hero-4.png) and it will be included randomly.
//
// No need to edit this file anymore for most cases.
// See the possibleHeroImages list in index.html if you need to add a custom name.

const heroImages = [];  // No longer used for the main list - kept for compatibility
